services:
  - type: worker
    name: fifi
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    envVars:
      - key: INTIGRITI_PAT
        sync: false
      - key: DISCORD_WEBHOOK_URL
        sync: false
